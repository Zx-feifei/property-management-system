{
  "name": "mock",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "mock": "json-server --watch data.json -p 8848",
    "test": "echo 'Error:no test specified' && exit 1"
  }
}